# Development and CI dependencies.
-r requirements.txt

pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0   # required by fastapi.testclient.TestClient
ruff>=0.5,<1.0

# Data and pipeline versioning. Only needed to run `dvc repro` or sweep
# experiments - the tests check dvc.yaml and params.yaml structurally, by
# parsing them, so CI validates the pipeline definition without executing it.
dvc>=3.50,<4.0

# Optional: only needed to run the streaming scorer against a real broker.
# The streaming logic itself is tested against an in-memory transport.
# kafka-python>=2.0,<3.0
